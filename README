# Cannot access due to Permissions
never run anything with sudo just run the `./apply-permissions.sh'

# Commit
Run `./get-permissions.sh` to update the `permissions.txt`
Run `git diff --binary` to get a patch that supports binary files

# checkout
Run `./apply-permissions.sh` after checkout to set the right permissions

